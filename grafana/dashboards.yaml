apiVersion: 1

providers:
- name: 'default' # does this do anything?
  folder: 'Home'
  type: file
  disableDeletion: false
  editable: true
  updateIntervalSeconds: 10
  allowUiUpdates: true
  options:
    path: /var/lib/grafana/dashboards